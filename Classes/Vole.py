


class Vole: 

    def __init__(self, tag, start_chamber, map): 
        self.tag  = tag 
        self.current_loc = start_chamber
        self.map = map 
    

    '''
    def send_move_request(self, destination) # replaces and expands on validity check
    def send_action_request(self, action) 
    def make_move(self, destination)
    def multichamber_move(self, goal)
    '''